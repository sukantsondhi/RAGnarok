# RAGnarok Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# EMBEDDING MODEL CONFIGURATION
# =============================================================================
# The embedding model converts text chunks into vectors for similarity search.
# Options:
#   - sentence-transformers/all-MiniLM-L6-v2 (80MB, 384 dims, fast)
#   - sentence-transformers/all-mpnet-base-v2 (420MB, 768 dims, accurate)
#   - sentence-transformers/multi-qa-MiniLM-L6-cos-v1 (80MB, 384 dims, Q&A optimized)
#   - sentence-transformers/paraphrase-MiniLM-L6-v2 (80MB, 384 dims, paraphrase)
EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2

# =============================================================================
# LLM MODEL CONFIGURATION
# =============================================================================
# The language model generates answers based on retrieved context.
# Options:
#   - google/flan-t5-small (300MB, 80M params, fast)
#   - google/flan-t5-base (990MB, 250M params, balanced)
#   - google/flan-t5-large (3GB, 780M params, high quality)
#   - google/flan-t5-xl (12GB, 3B params, best quality, requires high RAM)
LLM_MODEL_NAME=google/flan-t5-small

# =============================================================================
# MODEL STORAGE DIRECTORY
# =============================================================================
# Directory where downloaded models are stored locally.
# This prevents re-downloading models on each run.
MODELS_DIR=.models
